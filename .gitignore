out/
node_modules/
*.vsix
.vscode/
.vscode-test/
.superpowers/
.qodo
__pycache__/

# Local screen recordings used to cut the demo GIF. Kept out of git (they are
# large and regenerable); .vscodeignore already keeps them out of the VSIX.
media/*.mp4
